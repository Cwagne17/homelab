# =============================================================================
# ArgoCD Configuration
#
# This directory contains ArgoCD configuration manifests like RBAC policies,
# ingress, ConfigMaps, etc.
#
# TODO: Add configuration files:
# - argocd-cm.yaml (ConfigMap for ArgoCD settings)
# - argocd-rbac-cm.yaml (RBAC policies)
# - argocd-ingress.yaml (Ingress for ArgoCD UI/API)
# - argocd-notifications-cm.yaml (Notification settings)
# =============================================================================
---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: argocd

resources:
  []
  # TODO: Add ArgoCD configuration resources here
  # - argocd-cm.yaml
  # - argocd-rbac-cm.yaml
  # - argocd-ingress.yaml
